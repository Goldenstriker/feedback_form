name: Filters and Path Step

on:
  workflow_dispatch:
  
  pull_request:
    types:
      - opened
      - reopened
    paths:
      - "app/index.html"

jobs:
  take-action:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Test"